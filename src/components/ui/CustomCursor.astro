---
/**
 * Half-Life crowbar cursor. Pure CSS — no JS element tracking needed.
 * The SVG lives in public/cursors/ so it gets a stable URL for the CSS
 * cursor property. Hidden on touch-only devices via @media (pointer: fine).
 */
---

