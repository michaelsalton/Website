---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'p'>;

const { class: className, ...rest } = Astro.props;
---

<p class:list={['text-label text-content uppercase', className]} {...rest}>Michael Salton</p>
